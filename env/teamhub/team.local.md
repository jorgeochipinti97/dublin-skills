# Paths locales — __USER__ (per-máquina, GITIGNORED, nunca commitear)
#
# Mapea cada proyecto del REGISTRY a dónde lo tenés clonado en ESTA máquina.
# Formato: proyecto=/path/absoluto/al/repo
# `ds team-add` escribe acá automáticamente.
