# Private personal tasks — never commit
*.local.md

# Dublin env: pruned-skill backups + install backups (local only)
.dublin-orphans/
*.bak.*

# OS
.DS_Store

# Dependencies / build (uncomment per stack)
# node_modules/
# dist/
# .next/

# Secrets
.env
.env.local
